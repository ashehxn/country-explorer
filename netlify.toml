[build]
  command = "npm install && npm run build"
  publish = "dist"
  functions = "functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/server/:splat"
  status = 200
  force = true

[functions]
  external_node_modules = ["express", "mongoose", "connect-mongo"]
  node_bundler = "esbuild"